name: H1 OG Image Hotfix

on:
  push:
    paths:
      - '.github/workflows/h1-og-image-hotfix.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Patch OG image
        run: python3 .github/h1_og_image_patch.py
      - name: Verify and commit
        run: |
          git diff --check
          git diff -- humanity-live.html
          if git diff --quiet -- humanity-live.html; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "HUMANITY/1 Bot"
          git config user.email "actions@users.noreply.github.com"
          git add humanity-live.html
          git commit -m "Connect HUMANITY/1 launch social card"
          git push origin HEAD:main
